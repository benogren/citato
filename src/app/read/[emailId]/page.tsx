import Header from '../../../components/header';
// import { createClient } from "@/utils/supabase/server";


export default async function ReadEmailPage() {

    // const supabase = createClient();
    // const { data: emailContent } = await supabase
    //     .from('emails')
    //     .select('*')
    //     .eq('id', emailId);

    return (
        <>
            <Header />
        </>
    );
}